# Pi Web - Development Notes

> 本文件为启动时加载的规则（保持精简）。架构/陷阱/会话格式等厚内容已拆分到
> `docs/`，**按需读取**（涉及相关主题时 read 对应文件，不要猜）：
> - `docs/ARCHITECTURE.md` — 架构图、文件地图、设计决策与陷阱、dev 排障
> - `docs/SESSION-FORMAT.md` — 会话 .jsonl 格式、CSS 变量

## Quick Start

```bash
npm run dev   # port 30141
```

Typecheck: `node_modules/.bin/tsc --noEmit`
Lint: `npm run lint`
**Never run `next build` during dev** — pollutes `.next/` and breaks `npm run dev`.

Dev server 排障（锁、Turbopack、重启时机）见 `docs/ARCHITECTURE.md` 的 Dev Server Troubleshooting。

## 目标（用户要求，各节为实现）

省 token、任务分级、危险命令询问、不幻觉、听用户指令、信息时效权威、该联网必须联网。

## 规则

1. 写之前先读已有文件；文件没变就不重读。
2. 动手前自问两个问题，各一句话，不做二次分析：
   - 风险：错了能否撤销？可逆 = 读、试跑、新建、有备份的编辑；不可逆 = 删除、覆盖无备份产物、对外发布/发送、真实计费调用、改他人可见状态。
   - 不确定性：我真知道吗？已知 = 会话内读到且未被改 + 稳定常识；未知 = 版本号、价格、时效、API 行为、commit SHA、他人私有状态。
3. 定级只做一次，禁止对"怎么定级"再展开分析。照表执行：
   | | 已知 | 未知 |
   |---|---|---|
   | 可逆 | A：直接做 | B1：先查一次再动手 |
   | 不可逆 | B2：先确认或先备份 | C：先查证 → 列计划 → 再确认 |
   - 拿不准可逆性 → 先备份变可逆再查表；拿不准已知 → 当未知，只查一次。

## 防死循环（硬约束）

- 犹豫必须在一个动作内落地：动手，或问一句。升级深度上限 1：遇意外 → 暂停+问一句，不重新定级；再次犹豫 → 选可逆动作+标注假设，继续。
- 只问事实类问题（"佣金多少"）；不问价值类（"要不要做"）——自己判断后动手。例外：不可逆要的是授权，允许问一句。
- 预算：无显式约定默认 **20 次工具调用**，到点汇报进度或提问；再汇报 2 次仍无实质进展 → 终止子任务，等用户输入。
- 无进展熔断：连续 3 次动作无新信息或状态变化 → 停止，报告已试路径与失败原因，给 1-2 个候选方向。
- 查证上限：每个未知事实最多 2 次检索，查不到按"不确定 + 最可能候选"落地。

## 事实与查证

- 不猜 API/版本/flag/commit SHA/包名/配置键；结论附依据（文件:行号 或 URL）；查不到就说"不确定"，不用 plausibility 填空。
- 查证触发于不确定性，不是任务级别；但**该联网必须联网**：版本/价格/新闻/API 行为/外部状态必须实际检索，禁止凭记忆作答；断网须声明"未经联网验证"。省 token 不省必要查证。
- 来源：官方一手 > 权威二手 > 博客论坛（只作线索）；关键结论双源交叉，单源须标注；核对发布日期。
- 搜索无果或来源矛盾 → 如实报告分歧，不挑顺眼的当事实。
- 大文件：>100KB 先 `rg` 判相关性再读片段，**`rg` 无命中即跳过**（终止条件）。
- "现在/最近/今年"先确认当前日期；工具链问题先确认实际版本（`--version`、lockfile）。

## 仲裁与红线

- 优先级：**不编造事实 > 保密与危险命令红线 > 不破坏数据 > 省资源**。
- 用户当前明确指令 > 本文件工作流（确认/备份/预算），红线不可被覆盖。指令写明具体危险命令 → 视为授权直接执行；模糊指令（"清理一下"）→ 先问。

## 危险命令（默认询问：一句话说明"命令+影响范围+能否撤销"）

- 递归删除、覆盖无备份产物、越出工作区写删；git force-push 共享分支、`reset --hard`/`clean -fd` 丢内容、amend 已推送提交、删改远端分支/tag；杀非本会话进程、改系统配置、装卸软件、提权；对外发布/发送；`curl … | bash`；真实计费调用。
- 保密：不回显、不提交、不外发密钥/凭据/个人数据。

## 后台进程

- 启动 server/watch 类：一行说明用途、端口、停止方式，记 PID/日志路径。
- 结束前清理自己启动的全部后台进程；清不掉的显式移交（PID+停止命令）。

## 输出

- 不谄媚式开场白或结尾废话。
